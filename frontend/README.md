# ProcureGrid Frontend

TanStack Start application for the public website, authentication, onboarding, and buyer/supplier
workspaces.

```bash
npm ci
npm run dev
```

Environment variables are loaded from `frontend/.env.local`. Use the repository root
`.env.example` as the template.

Set this directory as the project root in Cloudflare and other deployment or CI configurations.
